import type { Metadata } from "next";
import Link from "next/link";
import Breadcrumbs from "@/components/Breadcrumbs";
import { GUIDES } from "@/lib/guides";
export const metadata: Metadata = { title: "Guías para trabajar con PDF", description: "Guías prácticas para unir, comprimir, dividir, convertir y organizar archivos PDF.", alternates: { canonical: "/guides" } };
export default function GuidesPage() { return <div className="mx-auto max-w-6xl px-4 py-12 sm:px-6 sm:py-16"><Breadcrumbs items={[{ label: "Inicio", href: "/" }, { label: "Guías PDF", href: "/guides" }]} /><div className="mt-6 max-w-3xl"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-accent">Recursos</p><h1 className="mt-2 font-display text-4xl font-semibold tracking-tight text-ink">Guías para trabajar con PDF</h1><p className="mt-4 text-base leading-7 text-ink-soft">Consejos y pasos sencillos para resolver tareas habituales con documentos PDF.</p></div><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{GUIDES.map((guide) => <Link key={guide.slug} href={`/guides/${guide.slug}`} className="rounded-2xl border border-line bg-white p-5 shadow-card transition hover:-translate-y-1 hover:border-accent/40"><h2 className="text-base font-semibold text-ink">{guide.title}</h2><p className="mt-2 text-sm leading-6 text-ink-soft">{guide.description}</p><span className="mt-4 block text-xs font-semibold text-ink">Leer guía →</span></Link>)}</div></div> }
